-- A sciprt that lists all record of second_table
-- Results should display both score & name in this order
-- Records should be order yb score (top first)
SELECT score, name FROM second_table ORDER BY score DESC;
